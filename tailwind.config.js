// tailwind.config.js
module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}", "./public/index.html"],
  theme: {
    // 主题配置
    extend: {
      colors: {
        "custom-red": "#d8fce9"
      },
    },
  },
  plugins: [
    // 插件配置
  ],
  variants: {
    // 变体配置
  },
  presets: [
    // 预设配置
  ],
};
